import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  text: {
    fontSize: '14px',
  },
  center: {
    textAlign: 'center',
  },
  th: {
    padding: '8px 20px',
  },
}));
